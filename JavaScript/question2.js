var min=window.prompt("enter the minutes");
if(min>0)
{
let sec=min*60;
document.write("the conversion of  "+min+"  minutes into seconds is "+sec)
}
else
{
document.write("cannot be determined")
}